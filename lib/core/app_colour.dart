import 'package:flutter/material.dart';

class AppColors {
  // Background Gradient
  static const backgroundTop = Color(0xFF2B1F00);
  static const backgroundMiddle = Color(0xFF111117);
  static const backgroundBottom = Color(0xFF070710);

  // Dotted Pattern
  static const dotColor = Color(0xFF8A7030);

  // UI Elements
  static const primaryGreen = Color(0xFF3BAD3B); // Action buttons
  static const darkGrey = Color(0xFF2C2C2C);     // Circle buttons
  static const footerText = Color(0xFF4A4530);   // Footer text

  // Feature & Gift Cards
  static const cardBg = Color(0xFF252520);
  static const cardBorder = Color(0xFF333328);
  static const iconBg = Color(0xFF1C1C1C);
  static const iconBorder = Color(0xFF3A3A30);
  
  // Accents & Gradients
  static const accentYellow = Color(0xFFFFCC00);
  static const accentYellowDark = Color(0xFFBF7400);

  // Wallet / Logo Colors
  static const walletPrimary = Color(0xFFE5C000);
  static const walletSecondary = Color(0xFFB89D00);
  static const walletGreenPrimary = Color(0xFF2E6D00);
  static const walletGreenSecondary = Color(0xFF133D00);

  // Confetti Particles
  static const confettiBlue = Color(0xFF2196F3);
  static const confettiYellow = Color(0xFFFFEB3B);
  static const confettiGreen = Color(0xFF4CAF50);
  static const confettiPink = Color(0xFFE91E63);
  static const confettiRed = Color(0xFFF44336);
}
